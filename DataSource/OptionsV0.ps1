# By default, StopClock4Work starts a break when workstation is locked (currently default is changed to False - restart is otherwise counted as break)
# With this option you can disable this
# Valid parameters are $True / $False

$AutoStopByLock = $True